plugins {
  id 'build.kotlin-publish-platform'
}

description = "Demo Bom"

javaPlatform {
  allowDependencies()
}

dependencies {
  constraints {
    file("${rootDir}").eachDirMatch(~/demo-.*/) { dir ->
      api(project(":${dir.name}"))
    }
  }
}