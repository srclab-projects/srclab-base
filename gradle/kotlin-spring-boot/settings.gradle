pluginManagement {
  repositories {
    gradlePluginPortal()
    jcenter()
  }
  plugins {
    id "org.jetbrains.kotlin.jvm" version "${kotlinVersion}"
  }
}

rootProject.name = 'demo-spring-boot-build'

file("${rootDir}/").eachDir { dir ->
  if (dir.name == "demo-spring-boot-starters") {
    dir.eachDir { subDir ->
      include ":${dir.name}:${subDir.name}"
    }
    return
  }
  if (dir.name.startsWith("demo-spring-boot-")) {
    include ":${dir.name}"
  }
}

include ":demo-app"