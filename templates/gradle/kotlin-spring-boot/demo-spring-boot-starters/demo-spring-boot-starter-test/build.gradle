plugins {
  id 'build.jar'
}

description = "Demo Spring Boot Starters for Test"

java {
  registerFeature('optionalSupport') {
    usingSourceSet(sourceSets.main)
  }
}

dependencies {
  api project(":demo-spring-boot-test")
}