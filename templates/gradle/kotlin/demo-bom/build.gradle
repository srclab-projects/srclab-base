plugins {
  id 'build.platform'
}

description = "Demo Bom"

javaPlatform {
  allowDependencies()
}

dependencies {
  constraints {
    file("${rootDir}").eachDirMatch(~/demo-.*/) { dir ->
      api project(":${dir.name}")
    }
  }
}